package edu.umg;

import edu.umg.Interfaz.FrInscripcion;
import org.hibernate.SessionFactory;
import org.hibernate.cfg.Configuration;

public class App {
    public static void main(String[] args) {
        // Configura el SessionFactory de Hibernate
      /*  SessionFactory sessionFactory = new Configuration().configure("hibernate.cfg.xml").buildSessionFactory();

        // Crea una instancia de FrInscripcion y pasa el SessionFactory
        FrInscripcion inscripcion = new FrInscripcion(sessionFactory);

        // Mostrar la ventana de inscripción
        inscripcion.setVisible(true);*/

        // No es necesario manejar la creación de cursos aquí

    }

}
